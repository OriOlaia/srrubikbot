// creando botones
const{ButtonBuilder} = require('discord.js')

module.exports = {
    description: 'Envía dos botones, uno envia usuario y el otro imagen',
    run: async (message) => {
        
    }
}